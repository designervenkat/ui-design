// Sort the elements of an array in place and returns the sorted array
// By default, it sorts the elements as strings, which can lead to unexpected results when sorting numbers.

// Sorting Arrays String and Numbers

// const fruits = ['Banana', 'Apple', 'Cherry']
// fruits.sort()
// console.log(fruits)

// const numbers = [5, 10, 1, 30]
// // numbers.sort() // not gonna work in numbers
// numbers.sort((a, b) => a - b)
// console.log(numbers)

// Sorting Objects

// const people = [
//     { name: 'Alice', age: 25 },
//     { name: 'Bob', age: 30 },
//     { name: 'Charlie', age: 22 },
// ]
// people.sort((a, b) => b.age - a.age)
// console.log(people)
