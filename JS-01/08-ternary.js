// Ternary Operator - short for if...else statements

// Basic syntax :

// condition ? experssion_if_true : experssion_if_false;

// const age = 15
// const canVote = age >= 18 ? 'Yes, you can vote!' : "No, You can't vote."
// console.log(canVote)

// const isMember = false
// const discount = isMember ? 20 : 5
// console.log(`Discount : ${discount}`)

// Nested Ternary Operator : if ... else if ... else
// const score = 91
// const grade = score >= 90 ? 'A' : score >= 75 ? 'B' : 'C'
// console.log(`Your grade is ${grade}`)

// Using functions
// function getAccess(role) {
//     return role === 'admin' ? 'Full Access' : 'Limited Access'
// }

// console.log(getAccess('user'))
// console.log(getAccess('admin'))
